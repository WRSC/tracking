<h1>Editing Attempt</h1>

<% if is_leaderById(Robot.find(@attempt.robot_id).team_id) || is_admin? %>

	<%= render 'form' %>

	<%= link_to 'Show', @attempt %> |

<% else %>

	<h2> You do not have the rights to access this page. </h2>
	
<% end %>

<%= link_to 'Back', attempts_path %>


