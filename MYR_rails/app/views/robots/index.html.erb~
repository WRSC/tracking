<h1>List of robots</h1><br>
<% if sign_in? %>
	<% if is_leader? || is_admin? %>
		<%= link_to 'New Robot', new_robot_path %>
	<% end %>
<% end %>
<table>
  <thead>
    <tr>
	
      <th>Name
		<a href= robots ><button id="sortbuttonT" name="NameRbis" onclick="myFunctionSort(this.name)" >a</button></a>
		<a href= robots ><button id="sortbuttonD" name="NameR" onclick="myFunctionSort(this.name)" ></button></a></th>	
	  
	  
      <th>Category
		<a href= robots ><button id="sortbuttonT" name="CategoryRbis" onclick="myFunctionSort(this.name)" ></button></a>
		<a href= robots ><button id="sortbuttonD" name="CategoryR" onclick="myFunctionSort(this.name)" ></button></a></th>		  
	  
	  
      <th>Tracker
		<a href= robots ><button id="sortbuttonT" name="TrackerRbis" onclick="myFunctionSort(this.name)" ></button></a>
		<a href= robots ><button id="sortbuttonD" name="TrackerR" onclick="myFunctionSort(this.name)" ></button></a></th>	
	  
	  
      <th>Team
		<a href= robots ><button id="sortbuttonT" name="TeamRbis" onclick="myFunctionSort(this.name)" ></button></a>
		<a href= robots ><button id="sortbuttonD" name="TeamR" onclick="myFunctionSort(this.name)" ></button></a></th>	  
	  
	  <% if sign_in? %>
		<% if is_admin? %>
			<th class="column-sed" colspan="2"></th>
		<% end %>
	<% end %>
    </tr>
  </thead>

  <tbody>
    
	<% idRobs = []%>
    <% @robots.each do |robot| %>
		<% idRobs << robot.id %>
	<% end %>


	<% allRobotIdSort=sortId(idRobs) %>
	<% if allRobotIdSort != [] %>
		<% for ii in 0..(allRobotIdSort.size-1) %>
			<% robot=Robot.find_by_id(allRobotIdSort[ii]) %>
		  <tr>
			<td><%= link_to robot.name, robot %></td>
			<td><%= robot.category %></td>
			<td><%= robot.tracker_id %></td>
			<td><%= Team.find(robot.team_id).name %></td>
			<% if sign_in? %>
				<% if is_admin? %>
					<td><%= link_to 'Edit', edit_robot_path(robot) %></td>
					<td><%= link_to 'Destroy', robot, method: :delete, data: { confirm: 'Are you sure?' } %></td>
				<% end %>
			<% end %>
		  </tr>
		<% end %>
    <% end %>
  </tbody>
</table>

<br>



<script>
	function myFunctionSort(clicked_id) {
		$.cookie("Sort",clicked_id,{path:'/' });
	}
</script>
