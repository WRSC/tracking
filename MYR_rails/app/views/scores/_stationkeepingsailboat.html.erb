<h2>Listing Station Keeping Category Sailboats' Scores </h2>
Finished <%= @sail_scores.length%> / All station keeping attempts <%= @sail_atts.length%>

<table id="datatable-score-stationkeeping-sailboat" class="dataTable display" >
  <thead>
    <tr >
        <th >Rank</th>
				<th>Team</th>
        <th >Robot</th>
				<th> Attempt</th>
        <th >Timecost</th>
        <th >Rawscore</th>
				<th >H I</th>
				<th >AIS</th>
				<th>Final Score</th>
			<% if sign_in? %>
				<th> Options</th>			
			<% end %>
    </tr>
  </thead>
  <tbody>
		<% @sail_scores.each do |score| %>
			<% if score != nil %>
			<tr>
		 		<% if score.attempt.robot.stationRank!=nil%>
					<% if score.id== score.attempt.robot.bestStationscoreId%>
		 				<td><%= score.attempt.robot.stationRank%></td>
					<% else %>
						<td>*</td>
					<% end %>
				<% else %>
					<td>?</td>
				<% end %>
				<td><%= score.attempt.robot.team.name %></td>
		   	<td><%= score.attempt.robot.name %></td>
				<td><%= score.attempt_id%>&nbsp;&nbsp;<%= score.attempt.name%></td>
				<td><%= score.timecost %></td>
				<td><%= score.rawscore %></td>
				<% if  score.humanintervention==0 %>
					<td>No</td>	
				<% else %>
					<td>Yes</td>	
				<% end %>
				<% if   score.AIS==0 %>
					<td>No</td>	
				<% else %>
					<td>Yes</td>	
				<% end %>
				<% if   score.finalscore!=nil %>
					<td><%= number_with_precision(score.finalscore, precision: 2) %></td>	
				<% else %>
					<td></td>	
				<% end %>   
				<% if sign_in? %>
		      <% if is_admin? %>
		        <td><%= link_to 'Edit', edit_score_path(score) %> <%=' | '%>
		        <%= link_to 'Delete', score, method: :delete, data: { confirm: 'Are you sure?' } %></td>
		      <% end %>
		    <% end %>
			</tr>  
			<% end %>
		<% end %>
  </tbody>
</table>
<br /><br />



