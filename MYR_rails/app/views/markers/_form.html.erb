<%= form_for(@marker) do |f| %>
  <% if @marker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@marker.errors.count, "error") %> prohibited this marker from being saved:</h2>

      <ul>
      <% @marker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div><br>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div><br>
  <div class="field">
    <%= f.label :latitude %>
    <%= f.text_field :latitude %>
  </div><br>
  <div class="field">
    <%= f.label :longitude %>
    <%= f.text_field :longitude %>
  </div><br>
  <div class="field">
    <%= f.label :datetime %>
    <%= f.datetime_select :datetime %>
  </div><br>
  <div class="field">
    <%= f.label :mission_id %>
    <% if Mission.all!=nil %>
        <%= f.collection_select(:mission_id, Mission.all, :id, :id) %>
      <% end %>
  </div><br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
