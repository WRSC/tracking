<% provide(:title, "Teams | New") %>
<h1>New Team</h1>

<% if sign_in? %>
	<% if !isInTeam? %>

		<%= render 'form' %>

	<% else %>

		<h2> You are already in a team.</h2>

	<% end %>

<% else %>
			<h2> You need to be logged in to access this page. </h2>
<% end %>

<%= link_to 'Back', teams_path %>
