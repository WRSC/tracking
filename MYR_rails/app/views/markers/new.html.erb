<% provide(:title, "Markers | New") %>
<h1>New Marker</h1>
<% if is_admin? %>
	
	<%= render 'form' %>

<% else %>

   	<h2>You cannot access this page. </h2>

<% end %>

<%= link_to 'Back', markers_path %>
