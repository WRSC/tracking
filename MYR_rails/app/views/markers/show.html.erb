<% provide(:title, "Markers | Show") %>
<p id="notice"><%= notice %></p>

<% if sign_in? %>
  <% if is_admin? %>

    <p>
      <strong>Name:</strong>
      <%= @marker.name %>
    </p>

    <p>
      <strong>Description:</strong>
      <%= @marker.description %>
    </p>

    <p>
      <strong>Latitude:</strong>
      <%= @marker.latitude %>
    </p>

    <p>
      <strong>Longitude:</strong>
      <%= @marker.longitude %>
    </p>

    <p>
      <strong>Datetime:</strong>
      <%= @marker.datetime %>
    </p>

    <p>
      <strong>Mission:</strong>
      <%= @marker.mission_id %>
    </p>

  <% else %>

    <h2>You cannot access this page. </h2>

  <% end %>
  <% else %>

    <h2>You cannot access this page. </h2>

<% end %>

<% if is_admin? %>
  <%= link_to 'Edit', edit_marker_path(@marker) %> |
  <%= link_to 'Delete', @marker, method: :delete, data: { confirm: 'Delete this coordinate ?' } %> |
  <%= link_to 'Back to index', markers_path %>
<% end %>